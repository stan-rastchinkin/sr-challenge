{
  "name": "mysterious-crawler",
  "version": "1.0.0",
  "description": "SR",
  "main": "index.js",
  "scripts": {
    "start": "dotenv -e .env -- node dist/index.js",
    "start:dev": "npm run build && npm run start",
    "build": "tsc --project tsconfig.build.json",
    "test": "dotenv -e .env -- vitest run --coverage"
  },
  "author": "SR",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.13.1",
    "@vitest/coverage-v8": "^1.3.1",
    "dotenv-cli": "^8.0.0",
    "nock": "^14.0.1",
    "typescript": "^5.4.2",
    "vitest": "^1.3.1"
  }
}
